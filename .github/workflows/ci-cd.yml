# name: BloodConnect CI/CD Pipeline

# on:
#   push:
#     branches: [ "main" ]
#   pull_request:
#     branches: [ "main" ]

# jobs:
#   test-frontend:
#     name: Frontend Tests (Vitest)
#     runs-on: ubuntu-latest
#     steps:
#       - uses: actions/checkout@v4
#       - name: Setup Node.js
#         uses: actions/setup-node@v4
#         with:
#           node-version: '20'
#           cache: 'npm'
#           cache-dependency-path: frontend/package-lock.json
#       - name: Install Dependencies
#         working-directory: ./frontend
#         run: npm ci
#       - name: Run Vitest
#         working-directory: ./frontend
#         run: npm run test -- --run # Runs tests once without watch mode

#   test-backend:
#     name: Backend Tests (Pytest)
#     runs-on: ubuntu-latest
#     steps:
#       - uses: actions/checkout@v4
#       - name: Setup Python
#         uses: actions/setup-python@v5
#         with:
#           python-version: '3.13'
#           cache: 'pip'
#       - name: Install Dependencies
#         working-directory: ./backend
#         run: |
#           python -m pip install --upgrade pip
#           pip install -r requirements.txt
#       - name: Run Pytest
#         working-directory: ./backend
#         run: pytest
#         env:
#           # Inject test environment variables
#           SECRET_KEY: test-key-do-not-use
#           DEBUG: True

#   deploy:
#     name: Production Deployment
#     runs-on: ubuntu-latest
#     needs: [test-frontend, test-backend] # CRITICAL: Wait for tests to pass!
#     if: github.ref == 'refs/heads/main' && github.event_name == 'push'
    
#     steps:
#       - name: Trigger Webhook Deployment
#         run: |
#           echo "Tests passed! Triggering zero-downtime rollout..."
#           # Replace this URL with the actual Deploy Hook URL provided by Render or Railway
#           curl -X POST "${{ secrets.DEPLOY_WEBHOOK_URL }}"
[pytest]
DJANGO_SETTINGS_MODULE = config.settings
# Tell pytest which files to look for
python_files = tests.py test_*.py *_tests.py
# (Optional) Disable warnings from third-party libraries for cleaner output
filterwarnings =
    ignore::DeprecationWarning